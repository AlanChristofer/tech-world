apiVersion: v1
kind: Service
metadata:
  name: portfolio-frontend
spec:
  type: NodePort
  selector: { app: portfolio-frontend }
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
